# This is end to end project
# Initiliase git ..  
git init
# publish repository
# Add file
git add abc.txt

git add .

git commit -m "my first commit"

#pull from git
git pull
git commit -m "Changes Added"
